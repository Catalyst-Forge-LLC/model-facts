---
description: Deploy the static site with pnpm ship
alwaysApply: true
---

# Deploy

Site publish is `pnpm ship` (Wrangler Pages project `model-facts`, uploads `site/` with no build step). Run `pnpm sync` in `directory-tools/` first if the catalog changed.
